import { Vector3 } from 'three'

export const kiteLibraryCubeMotion = {
  active: false,
  collisionRadius: 0,
  position: new Vector3(),
  previousPosition: new Vector3(),
  velocity: new Vector3(),
  waterContactOffset: 0,
}
